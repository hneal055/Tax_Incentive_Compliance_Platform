"""Startup script to initialize database on Railway deployment."""`nimport sys, os`nsys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))`nfrom app.db.session import engine, Base`nfrom app.models import Jurisdiction, Program`nfrom scripts.add_georgia import add_georgia`nfrom scripts.seed_georgia import seed_georgia`nimport logging`nlogging.basicConfig(level=logging.INFO)`nlogger = logging.getLogger(__name__)`ndef startup():`n    logger.info("🚀 Running startup initialization...")`n    Base.metadata.create_all(bind=engine)`n    logger.info("✅ Tables created")`n    add_georgia()`n    seed_georgia()`n    logger.info("✅ Startup complete!")`nif __name__ == "__main__":`n    startup()
