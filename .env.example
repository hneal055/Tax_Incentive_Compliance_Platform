# Tax Incentive Compliance Platform — Phase 1 Spine
# Copy to .env and adjust only if needed:
#   Copy-Item .env.example .env

APP_ENV=development
APP_HOST=127.0.0.1
APP_PORT=8000
LOG_LEVEL=INFO

# Rules directory override (optional)
# Default is: <repo_root>/rules
# TAX_RULES_DIR=rules

# Phase 2+ (database) — OPTIONAL in Phase 1
# DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/tax_incentives

# CORS
# ALLOWED_ORIGINS=*                          # or: https://example.com,https://admin.example.com
