server {
    listen 3000;  # replaced at startup by entrypoint.sh using $PORT
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — API calls go directly to backend (VITE_API_URL set at build time)
    location / {
        try_files $uri /index.html;
    }
}
