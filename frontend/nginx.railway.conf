server {
    listen 3000;  # replaced at startup by entrypoint.sh using $PORT
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — JS calls backend directly via VITE_API_URL baked in at build time
    location / {
        try_files $uri /index.html;
    }
}
