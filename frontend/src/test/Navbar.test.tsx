import { describe, it, expect } from "vitest"; import { render, screen, fireEvent } from "@testing-library/react"; import { BrowserRouter } from "react-router-dom"; import Navbar from "../components/Navbar"; const renderNavbar = () => render(<BrowserRouter><Navbar /></BrowserRouter>); describe("Navbar", () => { it("renders brand", () => { renderNavbar(); expect(screen.getByText("PilotForge")).toBeInTheDocument(); }); it("renders links", () => { renderNavbar(); expect(screen.getByText("Dashboard")).toBeInTheDocument(); expect(screen.getByText("Productions")).toBeInTheDocument(); }); });
