[build]
builder = "NIXPACKS"

[deploy]
startCommand = "prisma migrate deploy && uvicorn src.main:app --host 0.0.0.0 --port \"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
