[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh -c 'uvicorn src.main:app --host 0.0.0.0 --port $PORT'"
healthcheckPath = "/api/0.1.0/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
