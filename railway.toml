[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh -c 'cd frontend && npm install && npm run build && cd .. && uvicorn src.main:app --host 0.0.0.0 --port $PORT'"
restartPolicyType = "ON_FAILURE"
